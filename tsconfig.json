{
  "compilerOptions": {

    "module": "nodenext",
    // Node.js style module system (import/export Node অনুযায়ী কাজ করবে)

    "target": "esnext",
    // Compile করার পর জাভাস্ক্রিপ্ট latest version (ESNext) হবে

    "types": [],
    // কোন type declaration automatically load করা হবে (empty মানে কিছুই লোড হবে না)

    // ❌ Extra Garbage Files OFF
    "sourceMap": false,
    // .map debug files তৈরি করবে না

    "declaration": false,
    // .d.ts type definition ফাইল তৈরি হবে না

    "declarationMap": false,
    // .d.ts.map file তৈরি হবে না

    // ✔️ Strict Type Safety
    "noUncheckedIndexedAccess": true,
    // array[index] করলে value undefined হতে পারে — TS warning দেবে (extra safety)

    "exactOptionalPropertyTypes": false,
    // Optional properties একদম exact টাইপ follow করবে (undefined auto allow করবে না)

    "strict": true,
    // সব রকম strict type checking on — সবচেয়ে নিরাপদ TS configuration

    // ✔️ React + Module Options
    "jsx": "react-jsx",
    // React 17+ JSX transform enable (no need to import React)

    "verbatimModuleSyntax": true,
    // Import/export syntax 그대로 রাখবে (TypeScript rewrite করবে না)

    "isolatedModules": true,
    // প্রতিটি ফাইল আলাদা module হিসেবে treat হবে (Next.js, React, bundlers-এর জন্য perfect)

    "noUncheckedSideEffectImports": true,
    // Import করলে যদি কিছু side effect থাকে, TS warning দেবে (clean import)

    "moduleDetection": "force",
    // সব ফাইলকে force করে module হিসেবে ধরবে

    "skipLibCheck": true
    // node_modules-এর type file (.d.ts) গুলো check করবে না (build faster)
  }
}
